VITE_API_URL=https://aastu-event-hub.onrender.com/api

# This file is used for local production builds (vite). On Render you can set
# the same variable in the Static Site's Environment settings so builds use it.